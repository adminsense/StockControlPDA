@inherits LayoutComponentBase

<PageTitle>Stock Control — Admin</PageTitle>

<div class="shell">
    <header>
        <div>
            <h1>Stock Control — Admin</h1>
            <p class="sub">
                Mock (UX). Compact master-data registration flow. On PDAs, the scanner types into the focused field and may send <span class="mono">Enter</span>.
            </p>
        </div>
        <span class="pill">Mock</span>
    </header>

    <AdminTabs />

    @Body
</div>
